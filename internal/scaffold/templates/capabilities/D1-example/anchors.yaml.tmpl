# 能力域 → 代码锚点映射。命中路径的组件 PR 会收到 spec 提醒评论。
# 仓库键名必须与 sync/repos.yaml 中的登记一致。填写示例：
#
# capability: D1
# repos:
#   <repo-key>:
#     paths:
#       - test/integration/features/**
#     crds: [SomeKind]            # 可选：CRD 字段 diff 检测
capability: D1
repos: {}
