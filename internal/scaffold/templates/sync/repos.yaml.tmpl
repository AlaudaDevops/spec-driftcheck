# 组件仓库登记：anchors.yaml 的仓库键名在此解析为 git 地址。
# local: true 表示该仓库就是 spec 所在主仓库（driftcheck check 用 --local-repo-root 解析，免克隆）。
# 填写示例：
#
# repos:
#   <repo-key>:
#     url: https://github.com/<owner>/<repo>.git
#     branch: main
repos: {}
